// TabBarController.swift — БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ.
//
// Это была попытка сделать нижнюю панель через настоящий системный
// UIKit UITabBarController с iOS 26 UITab API. Сам таббар работал, но
// кнопка аватарки рядом с ним — нет: семь разных техник (bottomAccessory,
// ручная запись в tabBar.frame, расчёт от view.bounds, Auto Layout
// constraints к tabBar, контейнер, ужимающий весь экран через
// RootContainerController) так и не дали стабильного результата. Похоже,
// видимая плавающая капсула нового стиля iOS 26 рисуется отдельным
// системным слоем и/или переигрывается системой уже после наших изменений
// (внутренние анимации и т.п.) — мы боролись с недокументированным
// приватным поведением, а не с публичным API.
//
// Вернулись к самодельной SwiftUI-панели — см. RootView.swift, там теперь
// корень приложения (см. MangaLibApp.swift). Обычный HStack (капсула +
// кружок аватарки рядом) — расположение полностью в наших руках.
//
// У меня нет инструмента для удаления файлов — если хочешь, удали этот файл
// вручную в Xcode, он больше нигде не используется (не компилируется в
// граф зависимостей приложения).
