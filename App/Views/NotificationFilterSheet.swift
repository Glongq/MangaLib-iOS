// NotificationFilterSheet.swift — БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ.
//
// Была отдельная шторка (.sheet) с сортировкой/фильтром уведомлений.
// Попросили сделать кнопку сортировки нативным стеклянным `Menu` (как
// Сортировка в Каталоге/Комментариях), а не отдельным экраном — см.
// NotificationsView.filterButton, теперь это `Menu` с двумя `Picker`
// (Прочитанность/Порядок) напрямую по $viewModel.readFilter/sortOrder.
//
// У меня нет инструмента для удаления файлов — если хочешь, удали этот файл
// вручную в Xcode, он больше нигде не используется.
