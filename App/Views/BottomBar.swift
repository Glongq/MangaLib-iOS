// BottomBar.swift — БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ.
//
// Это была самодельная SwiftUI нижняя навигационная панель (в разных
// версиях: ручная геометрия + DragGesture + Metal-шейдер "капля воды", а
// затем GlassEffectContainer + .glassEffect + matchedGeometryEffect) — она
// пыталась ИМИТИРОВАТЬ Liquid Glass своими руками.
//
// Пользователь прислал референс кода из проекта Aidoku, показывающий, что
// на iOS 26 для этого есть настоящий системный API: UITabBarController +
// UITab. Он сам рисует таббар средствами ОС — без единой строчки кастомной
// отрисовки. Теперь это TabBarController.swift, а корень приложения —
// TabBarControllerRepresentable (см. MangaLibApp.swift). AppTab (enum,
// раньше объявленный здесь) тоже больше не нужен — переключение вкладок
// теперь делает сам UITabBarController.
//
// У меня нет инструмента для удаления файлов — если хочешь, удали этот файл
// вручную в Xcode, он больше нигде не используется.
