// LiquidGlassTabBar.swift — БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ.
//
// Эффект "жидкой капли" (metaball через Canvas + blur + alphaThreshold) для
// подсветки выбранной вкладки главной панели. Добавляли по отдельному
// запросу, но попросили вернуть панель как было — см. RootView.swift,
// FloatingTabBar (обычная статичная капсула без эффекта).
//
// У меня нет инструмента для удаления файлов — если хочешь, удали этот файл
// вручную в Xcode, он больше нигде не используется.
