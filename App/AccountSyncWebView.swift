// Файл больше не используется — заменён настоящим, подтверждённым эндпоинтом
// `POST /manga/{manga_id}/chapters/{chapter_id}/view`
// (см. MangaNetworkService.markChapterViewed, вызывается из
// ReaderViewModel.recordProgress). Раньше здесь жил WebView-хак
// (скрытая загрузка страницы главы в расчёте на побочный эффект), это было
// лишь приближением до того, как нашёлся реальный эндпоинт.
//
// У меня нет инструмента удалить файл из проекта — при желании удали его
// вручную в Xcode (правый клик → Delete → Move to Trash), он больше ни на
// что не ссылается и не используется.
